10-forwarding.conf
# ==== Forwarding defaults (deny) ===========================================
AllowTcpForwarding no
AllowStreamLocalForwarding no
AllowAgentForwarding no
PermitTunnel no
GatewayPorts no
X11Forwarding no
