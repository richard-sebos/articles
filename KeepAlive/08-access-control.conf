# Restrict SSH access to specific IP addresses or ranges

# Allow access only from trusted internal subnets or specific IPs
Match Address 192.168.100.0/24,10.0.0.0/8
    AllowGroups ssh-users

# Optional: add stricter controls per IP if needed
# Match Address 192.168.100.50
#     PermitRootLogin no
#     PasswordAuthentication no

# Deny all other sources by default if desired (not native to sshd_config)
# Use firewall (e.g., firewalld or iptables) to drop other IPs